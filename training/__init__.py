"""Training pipelines for game-playing agents. See README.md.

Currently houses the SFT harness (`training.sft`) that fine-tunes a small open model
(`Qwen/Qwen3.5-0.8B`) on the unified word-games distillation dataset via TRL `SFTTrainer`.
"""
