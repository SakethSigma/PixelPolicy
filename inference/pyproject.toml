[project]
name = "inference"
version = "0.1.0"
description = "Inference pipelines — run trained agents against games"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0",
    "httpx>=0.27.0",
]
